package assignment2;

// interface for cloning game entities, ensuring that all entities can be duplicated
public interface CloneableGameEntity {
    // method for cloning entity
    Object cloneEntity();
}
